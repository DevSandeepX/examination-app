import { Student } from "../models/schema.js"
const finishExam = async(req, res)=>{
    /*
    1. get the details from frontend --> isExamfinished, result, examStatus
    2. find the student with roll number 
    3. then set fields isExamFinished, result, examStatus then save the student
    4. return a response with success , message
    */
}